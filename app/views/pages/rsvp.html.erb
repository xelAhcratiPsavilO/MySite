<!DOCTYPE html>
<html class="rsvp" lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>rsvp</title>
  </head>
  <body>
    <br>
    <br>
    <div class="box" onsubmit="confirmSubmission()">
      <h2>RSVP</h2>
        <%= form_for :rsvp, url: rsvp_path do |f| %>
        <div class="inputBox">
          <%= f.text_field :name, :required => true%>
          <label>Name</label>
        </div>
        <div class="inputBox">
          <%= f.text_field :email, :required => true%><br>
          <label>Email</label>
        </div>
        <div class="inputBox">
          <select-label>Are you attending?</select-label>
          <%= f.select :attending, ['Yes','No'], :required => true%><br><br>
        </div>
        <div class="inputBox">
          <%= f.number_field :guests_number, :required => true%><br>
          <label>Number of guests</label>
        </div>
        <div class="inputBox">
        <select-label>Please list the guests</select-label>
          <%= f.text_area :companions, :required => true%><br><br>
        </div>
        <div class="inputBox">
          <%= f.text_field :dietary_restrictions, :required => true%><br>
          <label>Any allergies or food restrictions?</label>
        </div>
        <div class="inputBox">
        <select-label>Song suggestions?</select-label>
          <%= f.text_area :songs, :required => false%><br>
        </div>
        <br>
        <%= f.submit "Submit" %>
        <% end %>
    </div>
    <script>
      function confirmSubmission() {
        alert("The form was submitted");
      }
    </script>
  </body>
</html>
